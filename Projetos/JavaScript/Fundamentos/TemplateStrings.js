const nome = 'rebeca'
const conc = 'olá' + nome + '!'
// TEMPLATE
const template = `
   olá
   ${nome}`
   //expressoes...
   console.log(`1 + 1 = ${1 + 1}`)

   const up = texto => texto.toUpperCase()
   console.log(`Ei... ${up('cuidado')}!`)